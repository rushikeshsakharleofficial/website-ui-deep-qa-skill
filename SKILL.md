---
name: website-ui-deep-qa
description: Use when a website or web app needs QA — AI-generated or hand-built — and you see broken layout, unresponsive buttons, clipped content, broken forms, inaccessible elements, exposed tokens, poor mobile rendering, or missing error states. Also triggers on: post-deploy smoke test, PR visual review, or "does this site actually work?" audit.
---

# Website UI Deep QA

## Quick Reference — 46 helper categories

| Category | Helper | Artifact folder | Asserts HIGH? |
|----------|--------|----------------|---------------|
| Screenshots | screenshots.ts | screenshots/ | — |
| Layout issues | layout.ts | layout/ | ✅ |
| Content clipping | content-clipping.ts | content-clipping/ | ✅ |
| Scroll axes (H+V) | scroll-axes.ts | scroll-axes/ | ✅ |
| Zoom + scroll | zoom-scroll.ts | zoom-scroll/ | ✅ |
| Responsive | responsive-behavior.ts | responsive-behavior/ | ✅ |
| Interactions | interactions.ts | — | — |
| Button animations | button-animations.ts | button-animations/ | — |
| Forms | forms.ts | forms/ | — |
| Overlays | overlays.ts | overlays/ | — |
| Popup quality | popup-quality.ts | popup-quality/ | ✅ |
| Toasts | toasts.ts | toasts/ | — |
| Tables | tables.ts | tables/ | — |
| Carousels | carousel.ts | carousel/ | — |
| Media players | media-player.ts | media-player/ | — |
| Search | search.ts | search/ | — |
| Broken images | broken-images.ts | broken-images/ | ✅ |
| Lazy images | lazy-images.ts | lazy-images/ | ✅ |
| Accessibility | accessibility.ts | accessibility/ | ✅ |
| Keyboard focus | accessibility.ts | accessibility/ | — |
| Reduced motion | reduced-motion.ts | reduced-motion/ | — |
| Theme comparison | theme-comparison.ts | theme-comparison/ | ✅ |
| SEO | seo.ts | seo/ | — |
| PWA | pwa.ts | pwa/ | — |
| Security (DOM) | security.ts | security/ | ✅ |
| Network | network.ts | network/ | ✅ |
| Storage | storage.ts | storage/ | — |
| Console | console.ts | console/ | ✅ |
| Performance | performance.ts | performance/ | — |
| Auth surface | auth.ts | auth/ | ✅ (critical) |
| User lifecycle | user-lifecycle.ts | user-lifecycle/ | ✅ (critical) |
| Back/forward nav | back-forward.ts | back-forward/ | ✅ |
| Edge states | edge-states.ts | edge-states/ | ✅ |
| Placeholder content | placeholder-content.ts | placeholder-content/ | ✅ |
| Link checker | link-checker.ts | link-checker/ | ✅ |
| Cookie consent | cookie-consent.ts | cookie-consent/ | ✅ |
| HTML validation | html-validation.ts | html-validation/ | ✅ |
| CSRF | csrf.ts | csrf/ | — |
| Sitemap/robots | sitemap.ts | sitemap/ | — |
| Print media | print-media.ts | print-media/ | — |
| Sidebar | sidebar.ts | sidebar/ | ✅ |
| Dialog scroll | dialog-scroll.ts | dialog-scroll/ | ✅ |
| Form alignment | form-alignment.ts | form-alignment/ | ✅ |
| Typography | typography.ts | typography/ | ✅ |
| Auth permissions | auth-permissions.ts | auth-permissions/ | ✅ (critical) |
| Flow bypass | flow-bypass.ts | flow-bypass/ | ✅ |

All artifacts → `qa-artifacts/`. Fix plan → `qa-artifacts/reports/fix-plan.md`.

## Ecosystem

Part of the `code-real-builder` testing ecosystem. For multi-domain testing (UI + backend + security simultaneously), invoke `/code-real-builder` first. For UI-only testing, invoke this skill directly as `/code-real-builder:website-ui-deep-qa`.

## Requested task

$ARGUMENTS

If no explicit task is supplied, apply this skill to the current website, route, preview URL, local app, or repository context.

## Mission

Act as a strict QA engineer for a website or web app. The website may have been generated by Claude Code, Codex, Cursor, v0, Bolt, Lovable, Replit Agent, or any other AI/code generator. Assume the site may contain beautiful-looking but non-functional UI, fake buttons, placeholder links, broken responsiveness, leaked test data, poor accessibility, and unsafe browser storage.

Do not call the website passed until you have evidence.

## Non-negotiable rule

Do not randomly click around.

For every page:

1. Inspect first.
2. Create a page-specific plan.
3. Execute that plan in a controlled order.
4. Capture evidence.
5. Report exact defects.
6. State what was not tested.

## Preferred tool order

1. Use Playwright MCP for live exploratory UI testing when available.
2. Use the bundled Playwright Test files for repeatable automated testing.
3. Use source-code inspection when the repository is available.
4. Use manual instructions only for steps that cannot be safely automated.

## Safety boundaries

Never perform these without explicit user confirmation:

- payment
- booking or reservation
- public post
- sending email or messages
- destructive account changes
- deleting user data
- production deployment
- live database migration
- purchase, subscription, or billing change

Never ask the user to paste passwords, OTPs, tokens, API keys, card data, or secrets into chat. If login, 2FA, or payment is required, ask the user to take over the browser for that step.

Use fake test data unless the user explicitly provides safe test credentials or a staging environment.

## Initial assumptions to state

At the start of the report, state:

- target URL or local command used
- whether testing was unauthenticated or authenticated
- whether the environment looked like local, preview, staging, or production
- browser and viewport sizes tested
- whether destructive actions were skipped
- whether Playwright MCP, Playwright Test, or both were used

## Discovery and live planning

Discovery and planning happen together. As you discover each route and component, write its page test plan immediately — do not batch planning for after discovery is complete.

Order:
1. Navigate to the route.
2. Identify visible regions and interactive elements.
3. Write the page test plan for that route right now.
4. Execute the plan.
5. If discovery reveals a route or component type not already in `tests/deep-ui/helpers/routes.ts → seedRoutes` or not covered by existing helpers, record it in the "Not tested / Coverage gaps" section of the report and note which helper would need to be added or extended.

## Discovery phase

Before testing interactions, discover the app surface.

### Route discovery

Collect routes from:

- visible links
- sidebar links
- top navigation
- footer links
- buttons that navigate
- breadcrumbs
- router config
- `app/`, `pages/`, `src/routes/`, `src/app/`
- route constants
- `sitemap.xml`
- `robots.txt`
- `next.config.*`, `vite.config.*`, router files

Classify routes as:

- public
- auth-only
- admin-only
- settings
- detail page
- list/table page
- form/create page
- edit page
- error/not-found page
- marketing/landing page

### Component discovery

Identify every visible and potential UI component:

- left sidebar
- right sidebar
- top bar/header
- footer
- main content
- command palette
- floating action button
- floating chat/help widget
- cookie banner
- notification/toast
- alert/banner
- modal/dialog
- popover
- tooltip
- drawer/sheet
- dropdown/menu
- tabs
- accordion
- table/grid
- cards
- form
- search input
- filter panel
- pagination
- carousel
- upload/dropzone
- video/audio player
- calendar/date picker
- map
- charts
- auth/session controls

### Source-code inspection

If source is available, inspect:

- package scripts
- route files
- layout files
- shared UI components
- form handlers
- API client files
- auth/session files
- storage usage
- service worker/PWA files
- environment variable usage
- hardcoded test data
- placeholder links
- TODO/FIXME comments

Flag suspicious AI-generated leftovers:

- `href="#"`
- `alert("TODO")`
- `console.log` in production code
- mock data presented as real data
- hardcoded user profiles
- fake API responses
- buttons without handlers
- disabled submit without reason
- duplicate IDs
- clickable divs without keyboard support
- missing form labels
- default lorem ipsum
- exposed keys beginning with `sk_`, `pk_`, `AIza`, `ghp_`, `xoxb-`, `AKIA`, or JWT-looking strings

## Page-specific plan template

Before each page, write:

```md
## Page Test Plan: <page name>

URL:
<url>

Purpose:
<what this page should do>

Risk level:
<low / medium / high>

Visible regions:
- Left sidebar:
- Right sidebar:
- Top bar:
- Main content:
- Footer:
- Floating UI:
- Modal/popover candidates:
- Banners/toasts:

Screenshots:
- Top viewport
- Full page
- Scroll 25%
- Scroll 50%
- Scroll 75%
- Bottom
- After each meaningful interaction
- Mobile and tablet screenshots

Interactions:
- Navigation:
- Buttons:
- Forms:
- Search/filter/sort:
- Tables/pagination:
- Menus/dropdowns:
- Modals/drawers/popovers:
- Floating widgets:
- Auth/session actions:

Browser checks:
- Console errors
- Page exceptions
- Network failures
- Response leak scan
- Cookies
- localStorage
- sessionStorage
- IndexedDB
- Cache API
- Service workers
- Reload/back/forward behavior

Accessibility checks:
- Keyboard-only navigation
- Focus order
- Focus visible
- Dialog focus trap
- Escape behavior
- Labels and names
- ARIA misuse

Acceptance criteria:
- No broken layout
- No overlapping or clipped important content
- No dead controls
- No unsafe leaks
- No unexpected storage persistence
- No severe accessibility blocker
```

## Screenshot protocol

For every route and viewport:

1. Top viewport screenshot.
2. Full-page screenshot.
3. Scroll to 25% and screenshot.
4. Scroll to 50% and screenshot.
5. Scroll to 75% and screenshot.
6. Scroll to bottom and screenshot.
7. Return to top and screenshot if sticky/fixed UI changed.
8. Screenshot every opened modal, drawer, dropdown, popover, tooltip, toast, and floating window.
9. Screenshot every validation state.
10. Screenshot every loading, empty, error, and success state that can be reached safely.

Name screenshots predictably:

```text
qa-artifacts/screenshots/<route>/<viewport>/01-top.png
qa-artifacts/screenshots/<route>/<viewport>/02-full-page.png
qa-artifacts/screenshots/<route>/<viewport>/03-scroll-25.png
qa-artifacts/screenshots/<route>/<viewport>/04-scroll-50.png
qa-artifacts/screenshots/<route>/<viewport>/05-scroll-75.png
qa-artifacts/screenshots/<route>/<viewport>/06-bottom.png
qa-artifacts/screenshots/<route>/<viewport>/after-click-<element>.png
```

## Viewport matrix

At minimum test:

- desktop: 1440x900
- laptop: 1366x768
- tablet: 1024x768
- mobile large: 390x844
- mobile small: 360x640

If the site has a sidebar, test sidebar behavior at every breakpoint.

If the site has complex tables, test mobile overflow.

If the site has floating UI, test whether it covers controls on mobile.

## Cross-browser matrix

When Playwright Test is available, test:

- Chromium
- Firefox
- WebKit

If time is limited, run full coverage in Chromium and smoke coverage in Firefox/WebKit.

## Layout alignment checks

### Global checks

For every page:

- No unexpected horizontal scroll.
- No clipped important text.
- No overlapping controls.
- No content hidden under sticky headers.
- No content hidden behind sidebars.
- No content hidden behind footers.
- No floating widget covering CTA or submit button.
- No layout shift after loading completes.
- No inconsistent spacing between similar components.
- No inconsistent card height where uniformity is expected.
- No broken z-index layering.
- No invisible clickable layer blocking visible controls.

### Sidebar checks

Check each sidebar separately:

- correct edge attachment
- consistent width
- vertical alignment
- icon/text alignment
- active state
- hover state
- keyboard focus state
- collapsed state
- expanded state
- scroll behavior
- sticky behavior
- mobile drawer behavior
- backdrop behavior
- close button behavior
- Escape behavior
- outside-click behavior
- focus trap
- route highlighting after navigation
- no overlap with header or content

### Top bar checks

Verify:

- correct height
- correct width with/without sidebar
- logo alignment
- title alignment
- breadcrumbs alignment
- search input alignment
- avatar alignment
- notification alignment
- theme toggle alignment
- mobile hamburger behavior
- sticky/fixed behavior during scroll
- dropdown placement
- z-index over content but under modal
- no text overflow
- no controls hidden on mobile

### Footer checks

Verify:

- bottom placement on short pages
- natural flow on long pages
- column alignment
- link spacing
- legal text visibility
- mobile stacking
- no overlap with floating UI
- no hidden content behind sticky footer

### Main content checks

Verify:

- content start position
- section spacing
- heading hierarchy
- grid alignment
- card alignment
- image aspect ratios
- empty state alignment
- loading skeleton alignment
- table width
- chart overflow
- form label/input alignment
- validation text placement
- responsive wrapping

## Interaction checks

For every visible interactive element:

1. Identify role, accessible name, text, selector, and location.
2. Verify visible and enabled state.
3. Hover it.
4. Capture hover screenshot.
5. Focus it with keyboard if possible.
6. Capture focus screenshot.
7. Click it only if safe.
8. Wait for UI response and network response.
9. Capture resulting screenshot.
10. Verify expected result.
11. Verify no console/page error.
12. Verify no network failure.
13. Verify no duplicate unsafe request.
14. Return to prior state if needed.

Must cover:

- all buttons
- all links
- icon-only buttons
- dropdown triggers
- menu items
- tab controls
- accordions
- toggles
- checkboxes
- radios
- selects
- date pickers
- sliders
- pagination controls
- copy/download/upload controls
- sidebar navigation
- top bar navigation
- footer links
- floating buttons
- close/minimize controls

## Form testing

For every form:

### Basic states

- empty form
- invalid form
- partially valid form
- valid form
- loading state
- success state
- server error state
- network failure state if mockable

### Field values

Use fake values:

- empty string
- one character
- very long string
- emoji
- accented text
- special characters
- HTML-like input: `<script>alert(1)</script>`
- SQL-like input: `' OR 1=1 --`
- invalid email
- valid email
- invalid phone
- valid phone
- negative number
- zero
- decimal
- very large number
- invalid date
- past date
- future date
- short password
- long password

### Behavior checks

- submit empty form
- validation messages appear near the correct fields
- first invalid field receives focus where expected
- errors are announced/accessibly connected where possible
- submit button disables during loading
- double click does not create duplicate submission
- Enter key submits when expected
- Escape closes only overlays, not regular forms
- reset/cancel works
- unsaved changes warning works where expected
- file upload accepts expected types only
- password fields are masked
- password visibility toggle works
- copied values are not leaked into logs

Do not submit production contact, billing, ticket, or email-sending forms without explicit approval.

## Tables, lists, search, filters, and pagination

For every table/list:

- loading state
- empty state
- populated state
- long text wrapping
- column alignment
- sticky header behavior
- row hover
- row click
- checkbox selection
- bulk actions
- sort ascending/descending
- filter apply/clear
- search empty query
- search no results
- search special characters
- pagination next/previous
- page size selector
- infinite scroll loading
- duplicate row prevention
- mobile horizontal scroll or card conversion
- export/download button behavior if present

## Modals, dialogs, drawers, popovers

For every overlay:

- opens from correct trigger
- visible within viewport
- backdrop appears when expected
- focus moves inside overlay
- focus trap works for modal dialogs
- Escape closes where expected
- close button works
- outside click closes only when intended
- background scroll lock works for modal dialogs
- nested overlay behavior works
- z-index layering is correct
- mobile height and width fit
- internal scroll works
- form inside overlay validates correctly
- overlay cleans up after close

## Toasts and alerts

Check:

- success toast appears only after real success
- error toast appears on failure
- toast does not cover critical controls
- toast has readable contrast
- toast can be dismissed if long-lived
- repeated toasts do not stack infinitely
- alert banners have correct severity and close behavior

## Authentication and session checks

When authenticated testing is available:

- login success
- login invalid credentials
- logout clears sensitive state
- protected route redirects when logged out
- back button after logout does not reveal protected data
- refresh preserves session only when expected
- expired session behavior
- unauthorized API response handling
- role-based UI visibility
- admin links hidden from non-admin users
- user switch does not leak previous user's data

Never ask the user to paste credentials. Request browser takeover for login.

## Browser storage checks

Inspect before and after key interactions:

- cookies
- localStorage
- sessionStorage
- IndexedDB
- Cache API
- service workers

Flag:

- raw passwords
- access tokens in localStorage unless explicitly intended
- refresh tokens in JS-readable storage
- PII in cookies
- missing Secure on HTTPS cookies
- missing SameSite where relevant
- auth state not cleared on logout
- protected API responses cached after logout
- stale data after user switch
- large unexpected storage blobs

## Network checks

For every page and major interaction:

- no unexpected 4xx
- no unexpected 5xx
- no CORS errors
- no duplicate API calls due to rerenders
- no infinite polling unless documented
- no excessive payloads
- no token in query string
- no password/secret in request body unless intentionally submitting to auth endpoint over HTTPS
- no secret in response
- no internal stack trace
- no database error
- no debug-only response fields in production
- no cross-user data
- no unauthorized data before login
- no protected data after logout

Scan request URLs, request bodies where safe, response headers, and response bodies.

Sensitive patterns:

```text
password
passwd
secret
api_key
apikey
access_token
refresh_token
id_token
authorization
bearer
private_key
client_secret
session
cookie
jwt
database
stack
trace
exception
sql
mongodb
firebase
supabase
aws_secret
stripe_secret
ghp_
sk-
AKIA
AIza
xoxb-
```

## Console and runtime checks

Capture:

- console.error
- console.warn if severe
- uncaught exceptions
- unhandled promise rejections
- hydration mismatch
- React key warnings
- blocked mixed content
- CSP violations
- failed resource loads
- image 404s
- font 404s

Fail serious console errors unless documented as expected.

## Accessibility checks

At minimum:

- every interactive element has accessible name
- icon-only buttons have labels
- form inputs have labels
- errors are associated with fields
- headings follow logical order
- landmarks exist where appropriate
- keyboard-only navigation reaches all controls
- focus is visible
- focus order is logical
- modal focus trap works
- Escape behavior works
- skip link exists on complex pages where appropriate
- color contrast is not obviously poor
- no ARIA role misuse
- no duplicate IDs
- no focus lost after interaction

Keyboard sequence:

1. Start at page top.
2. Press Tab through all controls.
3. Press Shift+Tab back through important controls.
4. Use Enter/Space on buttons and links.
5. Use arrow keys in menus/tabs/selects where expected.
6. Use Escape to close overlays.

## Performance and stability checks

Check:

- initial page load time is reasonable
- no infinite loading spinner
- no repeated layout shift
- no excessive DOM growth on scroll
- no repeated duplicate network calls
- images are not obviously oversized
- loading states do not flicker indefinitely
- route transitions complete
- reload works
- back/forward navigation works

If Lighthouse is available, run it as an optional stronger check, but do not require it.

## Public page SEO checks

For public/marketing pages, check:

- unique title
- meta description
- canonical link when expected
- Open Graph tags when expected
- favicon loads
- robots meta is not accidentally `noindex` on production public pages
- heading structure
- important images have alt text
- links are crawlable where appropriate

## Error, empty, and edge states

Try to reach or simulate:

- 404 route
- unauthorized route
- server error state if safe/mockable
- empty list state
- no search results
- slow network loading
- offline state if service worker/PWA exists
- failed upload
- invalid file type
- expired session

## Visual regression

If baseline screenshots exist, compare against them. If not, create initial baselines only when the user wants regression baselines.

Do not mark a visual diff as a defect without confirming whether it is intentional.

## Required artifacts

Create or update:

```text
qa-artifacts/
  screenshots/          per-route, per-viewport PNG files
  network/              network records + leak findings JSON
  storage/              before/after storage state JSON
  console/              console errors + page errors JSON
  accessibility/        a11y issues + keyboard focus order JSON
  performance/          Web Vitals + DOM node count JSON
  layout/               layout issues per scroll position JSON
  forms/                form audit findings JSON
  overlays/             overlay audit findings JSON
  seo/                  SEO issues JSON
  security/             DOM + header + mixed content + URL token findings JSON
  reports/
    route-reports/
    final-report.md     per-route Markdown summary
  playwright-report/    HTML report (npm run report)
  results.json          machine-readable test results
```

## Defect format

Every defect must include:

```md
### DEFECT-<number>: <title>

Severity:
<Critical / High / Medium / Low>

Page:
<route>

Viewport:
<viewport>

Element:
<role/name/selector>

Steps to reproduce:
1. ...
2. ...
3. ...

Expected:
...

Actual:
...

Evidence:
- Screenshot: ...
- Network: ...
- Storage: ...
- Console: ...

Likely cause:
...

Recommendation:
...

Retest steps:
...
```

## Severity definitions

Critical:

- app/page cannot load
- auth/session broken
- cross-user data leak
- secret/token leak
- payment/security flow broken
- data loss
- destructive action fires unexpectedly

High:

- primary navigation broken
- main CTA broken
- form cannot submit
- modal/drawer traps user
- mobile layout unusable
- protected data visible after logout
- API 5xx on core flow
- serious accessibility blocker on primary flow

Medium:

- visible overlap affects readability or use
- dropdown/popover wrong position
- validation missing
- duplicate API calls
- table unusable on one viewport
- keyboard issue on non-critical flow

Low:

- minor spacing issue
- minor text wrapping
- cosmetic inconsistency
- non-critical icon alignment
- small contrast concern

## Final report format

End with:

```md
# Deep UI QA Report

Target:
<url>

Date:
<date>

Environment:
<local / preview / staging / production / unknown>

Authentication:
<unauthenticated / authenticated / partial>

Tools used:
<Playwright MCP / Playwright Test / source inspection>

Viewports:
- ...

## Summary

Overall status:
<PASS / FAIL / PARTIAL>

Pages tested:
<number>

Interactions tested:
<number>

Defects:
- Critical: <number>
- High: <number>
- Medium: <number>
- Low: <number>

## Coverage

- Routes:
- Layout regions:
- Forms:
- Tables/lists:
- Overlays:
- Floating UI:
- Network:
- Storage:
- Accessibility:
- Responsive:

## Defects

<defect list>

## Network findings

<summary>

## Storage findings

<summary>

## Accessibility findings

<summary>

## Performance/stability findings

<summary>

## Not tested

<exact limitations>

## Recommendation

<Ship / Do not ship / Ship after fixes / Needs retest>
```

## Supporting resources

Use these files when available:

- `resources/coverage-matrix.md`
- `resources/mcp-runbook.md`
- `resources/defect-template.md`
- `resources/security-storage-network-checklist.md`
- `resources/accessibility-checklist.md`
- `resources/responsive-checklist.md`
- `resources/report-template.md`


## Form automation

Run `auditForms(page)` from `helpers/forms.ts` to:

- Detect forms missing submit button
- Detect `action="#"` placeholder forms
- Flag password fields without `autocomplete`
- Flag disabled submit buttons on initial render

Run `triggerAndCaptureValidation(page, route)` to fire native HTML5 validation on empty forms and screenshot error states without submitting.

Do not submit forms to real endpoints. Use fake values only. Request browser takeover for login or payment.

## Overlay testing

Run `discoverAndAuditOverlays(page, route)` from `helpers/overlays.ts` to:

- Detect and click `aria-haspopup` triggers
- Audit open dialogs for missing `aria-modal`, `aria-labelledby`, and close button
- Test Escape dismissal
- Detect unbounded toast stacks

## SEO audit

Run `auditSeo(page)` from `helpers/seo.ts` on every public/marketing page to check:

- Title: present, length 10–80 chars, not a generic placeholder
- Meta description: present, length 50–165 chars
- Canonical link present
- Open Graph: og:title, og:description, og:image
- Twitter card
- Robots meta not accidentally set to `noindex`
- Favicon present
- Single non-empty `<h1>`
- Important images with alt text

## Security checks

Run `auditDomSecurity(page)` from `helpers/security.ts` to detect:

- Iframes without `sandbox` attribute
- `a[target="_blank"]` without `rel="noopener noreferrer"`
- Secrets in HTML comments
- Dangerous inline event handlers (`eval`, `Function(`)

Run `auditSecurityHeaders(responses)` to flag missing security headers:

- `Content-Security-Policy`
- `X-Frame-Options`
- `X-Content-Type-Options`
- `Referrer-Policy`
- `Permissions-Policy`

Run `scanUrlsForTokenLeaks(urls)` to catch tokens in query strings (JWTs, API keys, passwords).

Run `auditMixedContent(page)` on HTTPS pages to catch HTTP resource loads.

## Web Vitals

`collectPerformanceSnapshot(page)` now returns Core Web Vitals where the browser supports them:

| Metric | Good | Needs Improvement | Poor |
| --- | --- | --- | --- |
| TTFB | < 800ms | < 1800ms | ≥ 1800ms |
| FCP | < 1800ms | < 3000ms | ≥ 3000ms |
| LCP | < 2500ms | < 4000ms | ≥ 4000ms |
| CLS | < 0.1 | < 0.25 | ≥ 0.25 |

Use `poorWebVitals(snapshot)` to extract poor-rated entries. Report poor Web Vitals as Medium defects.

## Install into a target project

To run the bundled Playwright tests from inside the project you are QA-testing (instead of from the skill directory), copy the test files into that project:

```bash
bash /path/to/website-ui-deep-qa-skill/scripts/install-into-project.sh /path/to/your-project
```

The script copies `tests/deep-ui/`, `playwright.config.ts`, and `tsconfig.json` into the target project, and patches `package.json` with the required devDependencies and scripts. It skips any config file that already exists.

After install:

```bash
cd /path/to/your-project
npm install
npx playwright install
# Edit tests/deep-ui/helpers/routes.ts → add your routes to seedRoutes[]
BASE_URL=http://localhost:<port> npm test
```

## Bundled automation

This skill includes Playwright helpers under `tests/deep-ui/`. Use them for repeatable checks and expand them when the app has custom requirements.

`BASE_URL` defaults to `http://localhost:3000` when not set.

```bash
# Install dependencies and browsers (first time only)
npm install
npx playwright install

# Full suite — all viewports and browsers
BASE_URL=http://localhost:3000 npm test

# Chromium desktop 1440×900 only
BASE_URL=http://localhost:3000 npm run test:chromium

# Mobile 390×844 only
BASE_URL=http://localhost:3000 npm run test:mobile

# Headed mode (visible browser window)
BASE_URL=http://localhost:3000 npm run test:headed

# CI mode with GitHub reporter
BASE_URL=http://localhost:3000 npm run test:ci

# Open HTML report from last run
npm run report

# Type-check helpers without running tests
npm run typecheck

# Update visual regression baselines (only when intentional)
BASE_URL=http://localhost:3000 npx playwright test --update-snapshots
```

## Update tests when new things are found

If during testing you discover something not currently covered:

### New routes
If a route is visited that is not in `tests/deep-ui/helpers/routes.ts → seedRoutes[]`, add it to that array. State the addition in the final report under "Coverage updates".

### New component types
If a component type is encountered that no existing helper covers (e.g. a new overlay pattern, a custom widget, a third-party embed), do all of the following:
1. Test it manually using Playwright MCP or the spec's generic interaction helpers.
2. Document the component type, behavior, and any defects in the report.
3. Note which helper file should be extended or created (e.g. `helpers/embeds.ts` for third-party embeds) and describe what that helper should check.
4. If the discovery happens during automated spec execution, log it as a coverage gap in `qa-artifacts/reports/final-report.md`.

### New failure patterns
If a new class of defect is found that the helpers do not detect (e.g. a new type of token leak prefix, a new AI-generator placeholder pattern), add the pattern to the relevant checklist in `resources/` and note the addition in the final report.

Do not silently skip uncovered areas. Always report them.

## Token and context efficiency

These rules are mandatory. Violating them bloats context and degrades session quality.

### Never dump raw page content into context

- Do NOT call `page.content()` or read full HTML into the conversation.
- Do NOT print or summarise raw DOM trees. Use structured `page.evaluate()` queries that return only the fields you need.
- Do NOT read full artifact files back into context after writing them. `writeJsonArtifact` writes to disk — that is sufficient.
- Do NOT log or echo large arrays of findings inline — refer to the artifact path instead.

### Screenshot discipline

- Take screenshots only when there is a visible finding OR the protocol explicitly requires one (top, full-page, scroll positions).
- Do NOT screenshot the same state twice.
- Do NOT embed screenshot content in the conversation — reference the file path only.
- If no relevant elements exist for a check (e.g. no `<video>` on page), skip the screenshot for that check entirely.

### Early-exit on empty pages

Before running any helper, check if its target elements exist:

```typescript
// Example: skip media-player audit if no video/audio present
const hasMedia = await page.evaluate(() =>
  document.querySelectorAll('video, audio').length > 0
);
if (!hasMedia) return emptyReport;
```

Apply this pattern to: carousels, media players, tables, forms, toasts, overlays, search, auth surface, sidebar, dialog scroll, form alignment, auth permissions, flow bypass.

### Batch DOM queries

- Combine multiple related DOM checks into a **single `page.evaluate()` call** that returns all needed data.
- Do NOT make separate `page.evaluate()` calls for each field/property you need from the same element.
- One round-trip per helper section, not one per check.

### Deferred helpers — skip if no trigger

Run these helpers only when their trigger condition is confirmed:

| Helper | Skip unless |
|--------|-------------|
| `auditPrintMedia` | `@media print` stylesheet exists OR user requests it |
| `auditMediaPlayers` | `<video>` or `<audio>` or iframe embed detected |
| `auditCarousels` | carousel/slider element detected |
| `auditSearch` | search input detected |
| `auditCsrf` | `<form method="post">` or similar detected |
| `auditPWA` | `<link rel="manifest">` or service worker detected |
| `auditCookieConsent` | cookie-setting script or existing cookies detected |
| `auditToasts` | toast/alert role element detected |
| `auditSidebar` | `aside`, `[role="navigation"]`, or `[class*="sidebar"]` element detected |
| `auditDialogScroll` | `[role="dialog"]`, `dialog`, or `[class*="modal"]` element detected |
| `auditFormAlignment` | `form` or `[role="form"]` element detected |
| `auditAuthPermissions` | `input[type="password"]`, `[class*="login"]`, `[class*="permission"]`, `[data-role]`, or `[class*="logout"]` detected |
| `auditFlowBypass` | `[class*="wizard"]`, `[class*="stepper"]`, `[class*="paywall"]`, `[data-step]`, `form[novalidate]`, `input[type="hidden"][name*="step"]`, or `?step=` in URL detected |

### Artifact-first reporting

- Write all findings to JSON artifacts during the test run.
- In the final Claude Code session summary, report **counts and paths only** — not full finding objects.
- Use this pattern:

```
- Accessibility: 3 findings → qa-artifacts/accessibility/root-accessibility.json
- Security: 0 critical → qa-artifacts/security/root-security.json
- Broken images: 2 high → qa-artifacts/broken-images/root-broken-images.json
```

Never paste full JSON arrays into conversation output.

### Route deduplication

- Do NOT test the same normalised route twice.
- `discoverLinks()` already deduplicates — do not re-add seed routes that were already visited.
- If a route differs only in query string (e.g. `?page=1` vs `?page=2`), test only the base path unless pagination is the explicit target.

### Timeout minimums

Use the smallest timeout that reliably passes on the target app:

| Operation | Default | Minimum allowed |
|-----------|---------|----------------|
| `waitForLoadState('domcontentloaded')` | — | Required after each goto |
| `waitForTimeout` after goto | 700 ms | 300 ms for fast local apps |
| `waitForTimeout` after scroll | 300 ms | 100 ms |
| `waitForTimeout` after interaction | 300 ms | 200 ms |

Do NOT add `waitForTimeout` calls beyond these unless a specific race condition is being worked around.

### Context window hygiene

- Do NOT re-read a file you just wrote. Trust `writeJsonArtifact` and `Write` tool confirmations.
- Do NOT re-read the spec or helpers mid-run to "verify" them — load them once at the start.
- When a helper returns an empty result, log `0 findings` and move on without printing the empty array.
- Use the `advisor` tool only before substantive decisions — not to validate every helper call.
- Truncate long error messages to 200 characters when logging inline.

## Skill quality bar

This skill must remain:

- **Explicit** — every check is spelled out; do not rely on the model inferring intent.
- **Safe** — safety boundaries must never be removed or weakened.
- **Repeatable** — given the same site, two runs should follow the same methodology and produce comparable evidence.
- **Evidence-driven** — every defect requires screenshot, network, storage, or console evidence.
- **Suitable for weaker models** — instructions must be specific enough for a less capable model to follow without ambiguity.
- **Honest about untested areas** — every report must state what was not tested and why.

Never remove the non-negotiable inspect-first rule or any safety boundary from this file.
